import env from './env.js';

/**
 * Módulo de configuração principal da aplicação.
 *
 * @module config
 * @example
 * import config from './config/index.js';
 * console.log(config.apiPort);
 */
export default {
  ...env,
};
